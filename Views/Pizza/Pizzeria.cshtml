@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{

    Layout = "_Layout";
    ViewData["Title"] = "la mia pizzeria model";
}

@model la_mia_pizzeria_static.Models.pizzaList;

<h2> @Html.ActionLink("Crea La Mia Pizza", "CreaPizza", "Pizza")</h2>

<div>
    @foreach(Pizza p in Model.ListaPizze)
    {
        <div class="col-4 p-2 mb-3 card">

                    <div class="ciao">

                        <img src="~/img/pizza.jpg" />
                    </div>

                    <h2> Nome : @p.Nome</h2>
                    <br />
                    <span> Descrizione : @p.Desc</span>
                    <br />
                    <span> Prezzo : @p.Prezzo €</span>
                    <br />

                    <h4><a href="@Url.Action("ShowPizza", "Pizza", new {@p.Id})">@p.Nome</a></h4>
                    
                    @*<form asp-controller="Pizza" asp-action="Delete" asp-route-id="@p.Id" method="post">
	                @Html.AntiForgeryToken()
	                    <span class="text-danger">Elimina Pizza</span>
	                        <button type="submit" class="btn btn-danger btn-sm">
		                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-activity"><polyline points="3 6 5 6 21 6"></polyline><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path><line x1="10" y1="11" x2="10" y2="17"></line><line x1="14" y1="11" x2="14" y2="17"></line></svg>
	                        </button>
                    </form>*@
                    <a class="btn-danger btn d-inline" href="@Url.Action("RimuoviPizza", "Pizza", (Pizza)@p)">Cancella</a>

                    <a class="btn-warning btn d-inline" href="@Url.Action("AggiornaPizza", "Pizza", @p)">Aggiorna</a>

                </div>
    }
</div>

